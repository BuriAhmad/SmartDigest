[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
preDeployCommand = "alembic upgrade head && python -m app.cli seed_sources"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
